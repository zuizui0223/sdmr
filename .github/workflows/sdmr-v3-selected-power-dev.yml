name: sdmr-v3-selected-power-dev

on:
  push:
    branches:
      - 'design/sdmr-v3-process-identification'
    paths:
      - 'src/sdmr/process_id/known_truth/selected_power.py'
      - 'tests/test_process_id_selected_power.py'
      - '.github/workflows/sdmr-v3-selected-power-dev.yml'
  pull_request:
    paths:
      - 'src/sdmr/process_id/known_truth/selected_power.py'
      - 'tests/test_process_id_selected_power.py'
      - '.github/workflows/sdmr-v3-selected-power-dev.yml'

permissions:
  contents: read

concurrency:
  group: sdmr-v3-selected-power-dev-${{ github.ref }}
  cancel-in-progress: true

jobs:
  focused:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install test dependencies
        run: python -m pip install -e '.[test]'
      - name: Run selected-power tests
        run: pytest -q tests/test_process_id_selected_power.py
