name: sdmr-v5-integration-dev

on:
  push:
    branches:
      - 'design/sdmr-v3-process-identification'
    paths:
      - 'src/sdmr/process_id/known_truth/integration_v5.py'
      - 'tests/test_process_id_v5_integration.py'
      - '.github/workflows/sdmr-v5-integration-dev.yml'
  pull_request:
    paths:
      - 'src/sdmr/process_id/known_truth/integration_v5.py'
      - 'tests/test_process_id_v5_integration.py'

permissions:
  contents: read

concurrency:
  group: sdmr-v5-integration-dev-${{ github.ref }}
  cancel-in-progress: true

jobs:
  focused:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install test dependencies
        run: python -m pip install -e '.[test]'
      - name: Run v5 integration tests
        run: pytest -q tests/test_process_id_v5_integration.py
