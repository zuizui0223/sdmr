name: sdmr-v6-scoped-pipeline-dev

on:
  push:
    branches:
      - 'design/sdmr-v3-process-identification'
    paths:
      - 'src/sdmr/process_id/known_truth/scoped_pipeline_v6.py'
      - 'tests/test_process_id_scoped_pipeline_v6.py'
      - '.github/workflows/sdmr-v6-scoped-pipeline-dev.yml'
  pull_request:
    paths:
      - 'src/sdmr/process_id/known_truth/scoped_pipeline_v6.py'
      - 'tests/test_process_id_scoped_pipeline_v6.py'

permissions:
  contents: read

concurrency:
  group: sdmr-v6-scoped-pipeline-dev-${{ github.ref }}
  cancel-in-progress: true

jobs:
  focused:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install test dependencies
        run: python -m pip install -e '.[test]'
      - name: Run scoped pipeline tests
        run: pytest -q tests/test_process_id_scoped_pipeline_v6.py
