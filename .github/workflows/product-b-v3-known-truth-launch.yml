name: product-b-v3-known-truth-launch

on:
  push:
    branches: [main]
    paths:
      - 'configs/product_b_v3_known_truth_trigger.txt'

permissions:
  contents: write
  actions: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch exactly one frozen Product-B v3 known-truth run
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          python - <<'PY'
          import base64, json, os, time, urllib.error, urllib.request
          repo=os.environ['GITHUB_REPOSITORY']; token=os.environ['GH_TOKEN']
          workflow='product-b-v3-known-truth.yml'
          frozen_ref='frozen/product-b-v3-06350e55'
          implementation_sha='06350e55541f3ae0d846985edb196b68c536e2ab'
          api=f'https://api.github.com/repos/{repo}'
          headers={'Authorization':f'Bearer {token}','Accept':'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28','User-Agent':'sdmr-product-b-v3-launcher'}
          def req(url,*,method='GET',payload=None,allow_404=False):
              data=None if payload is None else json.dumps(payload).encode()
              try:
                  with urllib.request.urlopen(urllib.request.Request(url,data=data,headers=headers,method=method),timeout=60) as r:
                      raw=r.read(); return json.loads(raw) if raw else None
              except urllib.error.HTTPError as exc:
                  if allow_404 and exc.code==404: return None
                  raise
          def matches():
              p=req(f'{api}/actions/workflows/{workflow}/runs?event=workflow_dispatch&per_page=100')
              return [r for r in p.get('workflow_runs',[]) if str(r.get('head_sha'))==implementation_sha and str(r.get('head_branch'))==frozen_ref and str(r.get('event'))=='workflow_dispatch']
          runs=matches(); dispatched=False
          if len(runs)>1: raise SystemExit(f'multiple frozen Product-B v3 runs exist: {len(runs)}')
          if not runs:
              req(f'{api}/actions/workflows/{workflow}/dispatches',method='POST',payload={'ref':frozen_ref}); dispatched=True
              for _ in range(24):
                  time.sleep(5); runs=matches()
                  if runs: break
          if len(runs)!=1: raise SystemExit(f'expected one frozen Product-B v3 run, found {len(runs)}')
          run=runs[0]
          receipt={
              'purpose':'product_b_v3_known_truth_launch_receipt',
              'launcher_trigger_sha':os.environ['GITHUB_SHA'],
              'dispatched_now':dispatched,
              'run_id':int(run['id']),
              'run_number':int(run['run_number']),
              'status':str(run['status']),
              'conclusion':run.get('conclusion'),
              'created_at':str(run['created_at']),
              'head_sha':str(run['head_sha']),
              'head_branch':str(run['head_branch']),
              'implementation_sha':implementation_sha,
              'frozen_ref':frozen_ref,
              'workflow_name':str(run['name']),
              'html_url':str(run['html_url']),
              'opened_generating_truth_seed_maximum_before_run':523,
              'fresh_evaluation_seeds':list(range(701,713)),
          }
          path='configs/product_b_v3_known_truth_launch_receipt.json'
          old=req(f'{api}/contents/{path}?ref=main',allow_404=True)
          payload={'message':f'Record Product-B v3 known-truth run {run["id"]}','content':base64.b64encode((json.dumps(receipt,indent=2,sort_keys=True)+'\n').encode()).decode(),'branch':'main'}
          if old: payload['sha']=old['sha']
          req(f'{api}/contents/{path}',method='PUT',payload=payload)
          print(json.dumps(receipt,indent=2,sort_keys=True))
          PY
