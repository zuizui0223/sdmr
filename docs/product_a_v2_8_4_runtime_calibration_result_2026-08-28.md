# Product-A v2.8.4 runtime calibration result and freeze boundary

Status checked: 2026-08-28.

## Result

The truth-blind runtime calibration completed successfully in workflow run `33140419810` at calibration head `178f3b7cf161db4c6377376731cb205b07663c48`. All 21 predeclared `Poa annua` runtime cells completed. The maximum wall time was 5,637 seconds, yielding the predeclared proposal of a 225-minute scientific group-command timeout.

This is **runtime evidence only**. No sealed occurrence/environment outcome was opened, telemetry was not used for scientific selection, Product A was not promoted, and Product B was not unblocked. The cancelled v2.8.3 authoritative run remains a technical presealed terminal, not a favorable, null or adverse ecological result.

The decision artifact is pinned as artifact `9677721184`, digest `sha256:3742b48a6f964e2b599dc1c6fadfc656d1e41a4a34c539b0eefdd7144613fc6a`, with internal decision digest `1e9585c0c1a948c31df94bcc291bff94ad60594f580056d4f1211963aba29193`.

## Separate environment and timeout freeze

`configs/product_a_v2_8_4_environment_timeout_freeze.json` freezes:

- the unchanged v2.8.3 scientific contract and v2.8.4 runtime implementation identities;
- Python 3.12.11, direct dependency versions, a hash-locked dependency closure, and commit-pinned GitHub Actions;
- the calibration environment digest and the 225-minute command / 255-minute job timeout pair;
- all six immutable v2.8.3 model-pool and structural receipt artifacts;
- exact logical-shard, checkpoint, retry, and presealed-receipt identities;
- a separate one-shot sealed workflow barrier.

No post-launch timeout extension or scientific retuning is allowed.

## Current authorization boundary

This freeze does **not** authorize scientific execution. The next gate is a small, separate implementation and review of the presealed workflow plus its receipt barrier. Until that gate passes, `workflow_dispatch_allowed`, presealed execution, sealed execution, Product-A promotion and Product-B unblock remain false.

## Product-A hard stop

Once Product A reaches (1) one valid fresh empirical terminal decision under the unchanged full-denominator contract and (2) a separate explicit promotion/non-promotion decision, stop Product-A development and close the manuscript. Do not wait for Product B and do not hunt for a more favorable taxon, seed, M, threshold, candidate library, provider or cohort.
