# SDMR v3 prospective KT v2 — aborted unopened

Status: **aborted before prospective outcome access**

Prospective v2 inherited the complete scientific contract from unopened v1:
- seeds 33001–33020;
- Stage-P 8x random_cell shallow3;
- full-system information gate;
- frozen KT-A–KT-F gate vector.

Its development prerequisite was workflow run `35992320576`.

All eight scientific Stage-P shards in that run completed successfully. The workflow failed only in the aggregate step because the aggregate identity check hashed 17-digit CSV floating-point strings. The same positive-tail evidence differed from its predecessor only by ~1.1e-16 serialization roundoff; semantic states were identical.

Because v2 required the prerequisite workflow itself to terminate with `success`, it is not edited retroactively.

Therefore:
- prospective seeds 33001–33020 were never opened;
- no prospective outcome exists;
- no scientific threshold, model, sample size, world, or seed was changed;
- v2 is abandoned unopened;
- v3 inherits the already-frozen v2 science and changes only the prerequisite receipt to the aggregate-only repair workflow.
