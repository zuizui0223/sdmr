# Product-A v2.8.4 sealed pre-read recovery authorization review

This truth-blind review marker follows the generated static authorization for operational attempt 2.

The prior sealed run `33309627503` failed during the reusable authorization bootstrap before dependency installation, receipt download, sealed-source access, sealed-read state creation, or scientific decision. The reviewed repair is limited to invoking the authorization verifier as a standalone standard-library script. All scientific identities, source artifacts, presealed receipts, taxa, M, candidates, thresholds, RNG identities, fraction, denominator, and decision rules remain unchanged.

The recovery caller is restricted to `refs/heads/frozen/product-a-v2-8-4-sealed-v1-pre-read-recovery-1`, requires exactly two workflow-dispatch records (the pinned prior failure and the current recovery), and forbids a third dispatch. Scientific promotion remains false and Product B remains blocked.

This file does not authorize or read sealed ecological evidence. It exists to trigger ordinary pull-request CI on the fully generated authorization commit.
