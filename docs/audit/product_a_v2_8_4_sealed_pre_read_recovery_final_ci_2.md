# Final verifier-backed CI: Product-A v2.8.4 sealed pre-read recovery

This external commit triggers the repository's complete pull-request CI after the recovery authorization generator and fail-closed PR gate were hardened. The static authorization must pass the implementation verifier, preserve the prior pre-read failure identities, bind the caller and implementation hashes, retain all scientific invariants, and enforce an exact second dispatch with no third dispatch.

No sealed ecological evidence is accessed. Promotion remains false and Product B remains blocked.
